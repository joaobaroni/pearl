import 'package:get_it/get_it.dart';

final getIt = GetIt.instance;

Future<void> setupServiceLocator() async {
  // -- Hive Boxes --
  // Register Hive boxes here as features are added.

  // -- Repositories --

  // -- Use Cases --

  // -- ViewModels --
}
